[package]
name = "castaway-paths"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Where castaway keeps its files: one per-user directory layout, resolved for both the XDG and the Windows LocalAppData conventions."

[dependencies]
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
