[package]
name = "castaway-test-support"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "The deadline poll the workspace's tests share: a dev-dependency everywhere, a runtime dependency nowhere."

[dependencies]
tokio = { workspace = true, features = ["time"] }

[lints]
workspace = true
