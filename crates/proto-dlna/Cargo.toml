[package]
name = "proto-dlna"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "DLNA MediaRenderer: AVTransport + RenderingControl + ConnectionManager over SOAP, mounted on the shared SSDP/HTTP substrate."

[dependencies]
castaway-core.workspace = true
substrate-ssdp.workspace = true
thiserror.workspace = true
tracing.workspace = true
async-trait.workspace = true
tokio = { workspace = true, features = ["net", "sync", "rt", "macros", "time", "io-util"] }
axum.workspace = true
quick-xml.workspace = true
uuid.workspace = true
# The `SetAVTransportURI` HEAD probe (#99). Blocking, run on `spawn_blocking` — the
# same shape and the same crate `proto-gamestream` uses for NVHTTP.
ureq.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["net", "sync", "rt", "macros", "time", "io-util", "rt-multi-thread"] }
tower = { version = "0.5", features = ["util"] }

[lints]
workspace = true
